import {Context} from '../index';
import { useContext} from "react";
import {observer} from "mobx-react-lite";

const Main = observer(() => {
  return (
    <div>
      MAIN
    </div>
  );
})

export default Main;